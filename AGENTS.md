# ckhotgav-agy-tools — AI 助理協作守則

如果你是正在讀取此專案的 AI 助理，請務必遵守以下指導原則，以確保學生的學習體驗與系統安全。

---

## 🗣️ 溝通原則
1. **使用繁體中文**：所有回覆、說明、註釋與提示一律使用繁體中文（台灣習慣用語）。
2. **新手友善**：避免使用過度艱深的程式術語。如果必須使用（例如：API、Git、Environment Variable），請用極其通俗白話的方式解釋其用途。
3. **安全第一**：
   * 絕不能將使用者的 API Key、密碼、Token 硬編碼（Hardcode）寫入檔案。
   * 刪除檔案或覆蓋大段代碼前，必須先獲得使用者的確認。

---

## 💻 作業系統與環境處理
1. **終端機相容性**：
   * 預設以 **Windows** 系統 (PowerShell, `winget`) 為主撰寫。
   * 如果偵測到使用者是 **macOS** 或 **Linux**，在提供終端機指令時，應**自動轉換**為對應的指令（如將 `winget` 轉為 `brew`，將路徑中的反斜線 `\` 轉為斜線 `/`）。
2. **MCP 設定檔寫入**：
   * 在安裝 **Obsidian** (11) 或 **NotebookLM** (02) 技能時，若需要為使用者配置 MCP 連線：
     * **不要叫使用者手動去改 JSON 檔**。
     * 請主動讀取 `C:\Users\user\.gemini\antigravity\mcp_config.json`（Windows）或對應 Mac 的路徑。
     * 使用正確的 AGY2 格式：根鍵為 `"mcpServers"`，命令為單一字串（如 `"npx.cmd"` 或 `"npx"`），參數置於 `"args"` 陣列中。
     * 自動修改並儲存該檔案，最後提醒使用者重啟編輯器。

---

## 🔛 自動專案助理流程守則
* **開工 (Startup)**：
  * 開工只讀不寫，絕不修改使用者程式碼。
  * 優先掃描根目錄的 `PROJECT.md` 與當日 Obsidian 筆記，獲得完整的上下文後，再提出今日計畫。
* **收工 (Shutdown)**：
  * 必須是**互動式 (Interactive)** 詢問。列出選項，獲得使用者輸入「是」或點選確認後，才能執行 Git Commit/Push、或是 Netlify 部署、上傳 NotebookLM。絕不可自動靜默執行部署。
