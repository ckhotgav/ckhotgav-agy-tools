# ckhotgav-agy-tools 使用與開發指南

本指南詳細說明如何使用 AI 編輯器 (AntiGravity 2) 與本工具箱進行高效的程式開發與知識管理。

---

## 💡 使用概念：AI 雙腦協作 (AI Dual-Brain Co-working)
本工具包倡導「人負責想，AI 負責寫，工具負責同步」的模式。
* **本地腦 (Obsidian)**：用來整理自己的個人知識、日記與隨手筆記。
* **雲端腦 (NotebookLM)**：用來匯入專案文件、大文檔，供 AI 進行語意搜尋與深層分析。
* **AI 助理 (AntiGravity 2)**：連接這兩個腦，自動執行開工、程式碼修改、收工與部署。

---

## 🚀 啟動專案流程 (Project Initialization)
當您要從頭開始一個新的開發專案時，只需在 AntiGravity 2 中輸入以下任一對話關鍵字：
> 💬 **「啟動專案」**、**「專案啟動」** 或 **「開啟專案」**

---

### 📋 詳細操作步驟與對答說明

#### 1. 觸發專案助理
在對話框中輸入上面任一個關鍵字，例如：`幫我開啟專案`、`啟動專案`、`進行專案啟動`。

#### 2. 設定專案名稱
AI 助理收到指令後，會詢問您：
> *「嗨！我是您的自動專案助理。請問您要建立的新專案名稱是什麼？（例如：`my-web-app`）」*
* **您的操作**：直接輸入您想建立的專案資料夾名稱，建議使用英文、數字與減號 `-`，避免使用空格與特殊字元。

#### 3. 選擇專案模板類型
接著，AI 助理會詢問您要建立哪種類型的專案：
> *「好的，專案名稱為 `[專案名稱]`。請問這是一個什麼類型的專案？請回覆對應的編號或文字：*
> * *1️⃣ **靜態網頁** — HTML / CSS / JavaScript 網頁專案。*
> * *2️⃣ **Node.js 應用** — 使用 npm 管理的 JavaScript 後端或工具專案。*
> * *3️⃣ **Python 工具** — 用於數據處理、自動化的 Python 專案。*
> * *4️⃣ **文件／筆記專案** — 純寫文件與 Markdown 筆記的專案。」*
* **您的操作**：直接回覆 `1`、`2`、`3` 或 `4` 即可。

#### 4. 自動化建立與初始化
AI 助理會開始自動在當前的工作目錄下執行以下任務：
1. 建立對應專案的目錄結構（如 `src`、`assets` 等）。
2. 生成該類型的基本預設檔案（如 `index.html`、`main.py`、`package.json` 與 `.gitignore`）。
3. 自動建立專案管理核心檔案：
   * **`README.md`**：專案首頁，並包含「如何開工」的快速入門指引。
   * **`PROJECT.md`**：專案開發進度表，這是讓您能使用「開工/收工」流程的核心關鍵檔案。
4. 自動初始化 Git 版本控制：
   * 執行 `git init` 並設定預設分支為 `main`。
   * 自動執行 `git add .` 與 `git commit` 完成首次提交。

#### 5. 完成回報
完成後，AI 助理會印出產生的專案目錄結構，並以白話文提示您：
> *「🎉 專案已初始化成功！且已為您完成了首次 Git 提交。您隨時可以輸入『開工』開始開發囉！🚀」*
* **後續操作**：您可以直接輸入 **「開工」** 或 **「我來了」**，AI 助理將會立即讀取剛剛生成的 `PROJECT.md` 並為您開啟開發流程！

---

## 🌅 開工流程 (Startup Workflow)
每天開始工作時，只需在對話框中輸入：
> **「開工」** 或 **「我來了」**

AI 助理會自動在背景執行以下步驟：
1. **環境檢查**：確認當前目錄是否有未提交的變更，避免程式碼衝突。
2. **進度掌握**：自動讀取專案底下的 `PROJECT.md`，了解前一次開發進度與目前里程碑。
3. **日記讀取**：讀取 Obsidian 每日筆記，掌握今天的工作排程與代辦事項。
4. **狀態回報**：提供一個結構清晰的摘要，並詢問你：「我們今天準備從哪裡開始？」

---

## 🌌 收工流程 (Shutdown Workflow)
結束工作時，輸入：
> **「收工」** 或 **「下班了」**

AI 助理會引導你完成以下工作：
1. **自動存檔**：確認所有編輯中的檔案已儲存。
2. **進度更新**：自動將今日完成的任務更新至 `PROJECT.md`。
3. **日誌記錄**：自動將今日工作摘要與明日待辦事項寫入 Obsidian 每日筆記。
4. **選項式同步**（**AI 會問你，得到允許才執行**）：
   * 是否要 Commit 並 Push 到 GitHub？（AI 會自動幫你寫好 Commit 訊息）
   * 是否要部署到 Netlify 或雲端主機？
   * 是否要把今天的記錄同步到 NotebookLM？
5. **安全掃描**：確認你沒有在程式碼中硬編碼寫入 API Key 或密碼。

---

## 🛡️ 安全注意事項
1. **不要上傳金鑰**：千萬不要將 Supabase Key、Gemini API Key、Firebase 金鑰寫死在程式碼中。應使用 `.env` 檔案儲存，並確保 `.gitignore` 中有寫入 `.env`，避免將私密資料推送至 GitHub。
2. **確認後再刪除**：AI 在刪除任何檔案或重置程式碼前，必須先獲得你的確認。
